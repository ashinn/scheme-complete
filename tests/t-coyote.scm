(import (scheme base) (srfi 130))

(let* ((s "http://schemers.org/")
       (len (string-length s))
       (cat "cat")
       (colon (string-index s (lambda (ch) (eqv? ch #\:)))))
  (string-skip s (lambda (ch) (eqv? ch #\\)) c l
  (+ 2 
